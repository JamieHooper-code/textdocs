15. I am extremely interested in figuring out some way to highlight text on my screen better. For example you just sent me that PowerShell link, I don't use a mouse at all. That is very hard to make it copy to my clipboard. I told you to remember to automatically copy stuff to make the board but you always forget, but that is a separate issue. This is an issue I encounter in a variety of applications and websites. I would really like to brainstorm this and potentially make a tool or find one that would help with this. I would like you to be as efficient and easy to use as possible and with as much power as possible. I've used a few different things for this like Legion that is built into caster and it just didn't quite work super well but I'm also interested in ways that we can prove that.
---
14. Stream decks Json importing and exporting. Maybe an issue with editing inside of the app if it loses state
---
13. Adding World of Warcraft YouTube MP3s to my lockout timer. Could make it so that it cycles through each different database I have per week.
---
12. Consider putting my add to send functions through haiku or sonnet to have it auto generate the responses. Also the true/false thing should just be like a selector not an input field..........  also the ad to stream decks thing should difficult to no
---
11. I need to come up with a better food system
---
10. Process all of my quotes and tag them. Make it so that I can add them very easily with tags.
---
9. Fix my adding notepad files, for some reason it was not detecting my quotes. At least find some way to easily get the location of the file.
---
8. what is the best way to shut down my computer in terms of Dragon and caster?
---
7. Tell me about Visual Studio code insiders and its recent implementation of cloud code agents.
---
6. Improve my calendar commands and at reminders
---
5. At my obsidian fault to my auto hotkey caster workspace
---
4. Add commands like add that to my Dragon dictation box commands
---
3. Make a way to add the current directory as a macro of like add directory
---
2. tell me more about the issue of my RAM not being properly configured
---
1. {
  "command": "Start-Process MAINFUN.bat -ArgumentList 'OpenSearchExe', 'C:\\Program Files\\Everything\\Everything.exe', 'ahk_exe Everything.exe', '-search \"test query\"' -WindowStyle Hidden; Start-Sleep 4; Get-Content \"$env:TEMP\\ahk_last_result.txt\"",
  "description": "Test OpenSearchExe directly with Everything"
}

When you run these tests I get little window pop-ups with the errors is there way to get them to show silently