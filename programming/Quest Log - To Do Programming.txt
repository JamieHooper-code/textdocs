~~~ QUEST LOG ~~~
              . Programming Adventures .

Greetings, traveler. Bound within these pages are the quests that lie
before you — some grand and many-chaptered, others humble errands for
a rainy afternoon. Contracts sworn to the Atlantic Logistics guild sit
alongside personal undertakings: warding rituals for the vault,
enchantments for the timer overlay, and wild tinkering upon the AHK
and Caster arcana.

Short quests are inscribed here as a title and a few lines of counsel.
The longer sagas — those demanding blueprints, trade-offs, and tables
of lore — are kept in their own scrolls beneath:
    TEXTDOCS/programming/to do explanations/<TOPIC>.md
Each such quest bears a "Full design:" line pointing the way to its
full scroll. Keep this ledger scannable; let the deep lore live in the
linked files.

May your builds be green and your hotkeys unbound. Onward.

================================================================
ATLANTIC LOGISTICS
================================================================

6. Outbound email metadata automation
   Auto-populate fields, formatting, signatures based on context when
   sending.

8. AI Load Email to McLeod — active campaign
   The big Atlantic build: parse customer load emails, post them to
   McLeod as Subject Orders, keep them reconciled as loads change.
   Full quest breakdown (Active Quests, Wayside Errands, Sealed
   Chambers, Awaiting Ravens) lives in its own scroll. Update THAT
   file as work moves between categories; this entry is just the
   doorway.
   Full design: Atlantic Logistics/AI Load Email to McLeod/docs/TO_DO_AI_LOAD_EMAIL_MCLEOD.md


================================================================
PERSONAL
================================================================

--- Research / Tooling ---

0. Obsidian vault — next steps (auto-tagging, ports, tooling)
   Vault stood up at Important/ObsidianVault. SaveCurrentLink writes
   markdown bullets directly with a tag-picker Gui (and edit mode if
   the URL is already saved). SavedLinks/*.txt migrated. Headline next
   move: retroactive auto-tagging of the migrated bullets. Also: port
   more text resources, build vault-management scripts (tag rename /
   link check / search-from-voice / browse-by-tag), explore Dataview
   dashboards, daily notes, web clipper, mobile sync. (Notion was
   evaluated and rejected — Obsidian fits the local-files + voice
   workflow better.)
   Full design: TEXTDOCS/programming/HANDOFF - Obsidian Next Steps.md


--- Backups / Security ---

1. Slot encryption key — third backup location
   Key file: Scripts/CopyPasteSlotCrypt/copy_paste_slot_crypt.key.
   Currently backed up to AutoHotkey folder + E: drive. Add a third
   (Charli's machine, USB, or password manager attachment). Without the
   key every encrypted slot is permanently unrecoverable.

2. Atlantic AI Load Email secrets — third backup location
   Currently only on laptop + E: drive at "Important Backups/Atlantic
   Logistics AI Load Email/". Contains the McLeod API key, the Graph
   service-principal .env, and the .pfx private key for the
   subjectorders@shipatlantic.com mailbox cert. Same single-point-of-
   failure problem as the slot key: if E: dies and the laptop dies,
   these are gone. Add a third copy (Charli's machine, second USB, or
   encrypted cloud vault). Do this before the pipeline is load-bearing
   in production. The Bitwarden task below would also solve this if
   it gets done first.

3. Bitwarden as credential vault for client work
   Slots are great for voice-first secrets but lack metadata, TOTP, mobile
   access, recovery. Add Bitwarden (free tier) as authoritative vault for
   Atlantic/McLeod/Microsoft creds. Keep slots for daily-use keys, bridge
   via a one-way SyncSlotFromBitwarden helper. AHK-first, new
   Helpers/CredentialsFunctions.ahk.
   Full design: TEXTDOCS/programming/BITWARDEN_CREDENTIAL_SETUP.md
   Prereq: sign up + 2FA + print recovery kit + back it up to E:.


--- Timer Overlay (TimerOverlay.ahk / TimerAudio.py) ---

3. Quotes display
   Add Scripts/quotes.txt (one per line). Pick random at start or rotate
   on tick, display under song name.

4. Multiple sound folders
   Either single variable at top of TimerAudio.py, or a weighted list of
   folder paths picked per track. Candidates: OSRS, other game OSTs,
   lo-fi. Adventure.ogg end-track could vary by active set.

5. Move OSRS Sounds to second drive
   Once set up. Update soundsDir path in TimerOverlay.ahk (~line 38).

6. Lockout timer — future ideas: AutoHotkey/Scripts/LockoutTimer/FUTURE_IDEAS.md
7. Habit RPG (new project): projects/habit-rpg/BUILD_PLAN.md — WoW-themed
   gamification for real skill practice, characters personify practice
   domains, lockout timer is one input source among many


--- AHK / Caster ---

6. Alarm Clock App — test refactor end-to-end
   Say "enable AlarmClockRule", verify each voice command (set timer /
   alarm / stopwatch / show timers / cancel all). Rule:
   caster/rules/alarm_clock_commands.py. Logic:
   Helpers/AlarmClockFunctions.ahk. Server:
   Scripts/AlarmClockApp/alarm_clock_server.py (localhost:7459).
   Replace generated sounds if bad, add presets, consider SD buttons.

7. Calendar voice commands — natural language reminders
   "remind me in three months / two weeks / tomorrow at X / next Friday".
   AHK-first in a new Helpers/CalendarFunctions.ahk. Backend: Google
   Calendar (gcal MCP already wired), Windows Reminders, or Alarm Clock
   for short horizons. Handle minutes/hours/days/weeks/months/years.

8. AHK MCP introspection server
   Structured on-demand access to AHK function catalog + Caster rules.
   Better than pre-loading memory when catalog grows. FastMCP (Python 3).
   Not urgent — do when verify.ps1 + memory files feel unwieldy.
   Full concept: TEXTDOCS/programming/Debugging/AHK_MCP_INTROSPECTION_SERVER.txt

9. Dragon / Natlink — safe kill + restart
   Current kill is unsafe. Investigate Dragon's shutdown API, UI
   automation, or staged kill that waits for Natlink to unload. Hide/close
   Natlink window during shutdown. Goal: no orphaned processes, no frozen
   backlog.
   Diagnostic info: Dragon_NATLINK_debugging.txt

10. Dragon vocabulary import
    Current flow for adding words is bad. Look into vocabulary import.

11. Google Docs combo keypress timing bug
    Combos like "lefty clear to sauce" fire too fast; Docs reads them as
    Ctrl+Home/End. Add delays or SetKeyDelay scoped to Docs context.

12. Google Voice — "text <name>" for contacts not on first page
    URL-based navigation (itemId) fails when thread isn't in the lazy
    list. Switch to search: click search, type name, click result. See
    TODO in GoogleVoice.ahk.

13. Temporary voice command scoping
    Limit active voice commands to a specific rule file or group on
    demand — via voice ("only VS Code commands", "scope to git", "unscope")
    or external trigger (Stream Deck, AHK, CLI). Goal: silence unrelated
    rules during focused work without disabling them permanently. Decide
    mechanism: toggle Caster rule enable/disable, a global gate checked
    in rdescript, or a context filter. Needs a visible indicator of
    current scope and an always-on escape phrase.

14. AHK function testing tooling for Claude
    Build verify.ps1 ahk-run subcommand (Layer 1) + eventual ahk-test
    skill (Layer 2) so Claude can fire AHK functions one-shot and verify
    they actually did something.
    Full design: TEXTDOCS/programming/to do explanations/AHK_FUNCTION_TESTING.md


--- New Tab Page ---

15. New tab page improvements
    a. Check-off tasks that reset counters (e.g. journaling -> 5 days)
    b. User profiles with Google integration
    c. Image debugging workflow
    d. General settings page
    e. Panel for current YouTube/tabs/activities
    f. Make it work from anywhere (at least speech)
    g. Poem/quote of the day dynamically pulled from external sources
    h. Weather panel
    i. Panels as easy-to-add options across boards
    j. Recurring event/task panel with reset-on-check
    k. Portable to-do list sourced from elsewhere
    l. Nested checklists (click one to reveal sub-tasks)


--- Hardware / Storage ---

16. Storage migration + cloud backup
    a. Fix downloads folder + hard drive situation
       Recover stuff off the 1 TB drive. Once second drive is set up:
       move OSRS Sounds and update soundsDir in TimerOverlay.ahk. Also
       move CountdownTimer tooltip images (Scripts/CountdownTimer/
       wow_items/tooltips/, 700+ PNGs and growing) off SSD; update
       tooltip path in TimerOverlay.ahk.
    b. Move poem-poster media folders to second drive
       projects/poem-poster/ has ~27 GB of media — clipart/ (27 GB,
       ~38k files) is the main offender. Also candidates: backgrounds/
       (44 MB), inspiration/ (62 MB), out/ (244 MB), completed/ (61 MB).
       Keep code + poems/ + templates/ on SSD. Update path constants in
       the poem-poster scripts after moving.
    c. Add OneDrive as a third backup mirror (after E:)
       Extend git_push_config.json + GitPushAll.ps1 so each E: mirror
       target can also copy to C:\Users\jamie\OneDrive\Important Backups\.
       CRITICAL exclusions: no .atlantic_graph_certs, no keyfile, no
       .env/.ssh/.aws creds, no plaintext API keys. Turn OneDrive Known
       Folder Move OFF first or every hardcoded path breaks.
       Full design + security rationale:
       TEXTDOCS/programming/to do explanations/ONEDRIVE_BACKUP.md


18. Install the RTX 2070 (sitting on the desk)
    Physical install + NVIDIA driver + CUDA Toolkit, then node-llama-
    cpp rebuild for CUDA backend so QMD (and any future local-LLM
    tooling) gets real GPU inference. Current iGPU eats 60s on a
    cold qmd query; 2070 + warm daemon should land ~2-4s. Also
    unlocks Ollama, LM Studio, small-model finetuning — general
    upgrade, not just QMD. Pre-install: check PSU wattage (≥550W),
    PCIe power connector, slot clearance. Claude can handle the
    software side (rebuild + daemon + benchmarks) once drivers are
    live; physical install is a screwdriver job.
    Full design: TEXTDOCS/programming/to do explanations/INSTALL_RTX_2070.md


--- Verify / Testing Infrastructure ---

17. Caster/AHK verify system — next improvements
    Built 2026-04-22: Stop-hook + Caster<->AHK bridge contract (both
    directions) + pytest suite. Four queued extensions, pick any
    independently: AHK LSP integration (catches runtime #Warn / undefined
    locals that /validate misses — the footpedals pop-up class), auto-
    generated codebase inventory (proactive — Claude reads it before
    writing new helpers, so no more duplicate _snake_case reinventions),
    template skills for recurring workflows (add-voice-command,
    migrate-choice-dict-to-json, add-streamdeck-button — build on next
    use, not speculatively), and AI-generated commit messages via
    Claude CLI with --model haiku in GitPushAll.ps1 (preserves Opus
    quota, kills the "updates" × N git history problem). Umbrella:
    keep adding tests + docs + checks as gaps surface in use.
    Full design: TEXTDOCS/programming/to do explanations/CASTER_AHK_VERIFY_IMPROVEMENTS.md
