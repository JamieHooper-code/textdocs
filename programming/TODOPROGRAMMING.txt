--- ENCRYPTION KEY BACKUPS ---

Make another backup of the slot encryption key (and ideally the encrypted slots too)
in a third location besides the AutoHotkey folder and the E: external drive.
Candidates: Charli's machine/drive, a USB stick, a password manager attachment.
Without the key file (Scripts/CopyPasteSlotCrypt/copy_paste_slot_crypt.key) every
encrypted slot is permanently unrecoverable.

--- TIMER OVERLAY (TimerOverlay.ahk / TimerAudio.py) ---



1. Quotes display
   - Add a quotes.txt file in Scripts/ (one quote per line)
   - Pick a random quote at timer start and display it beneath the song name
   - Or rotate quotes on each tick

2. Multiple sound folders
   - Option A: Edit a single variable at the top of TimerAudio.py to point to a different folder
   - Option B: Pick randomly from multiple folders — define a list of folder paths in Python,
     pick a folder per track (weighted or equal chance), then pick a track from that folder
   - Folders could be: OSRS Sounds, other game OSTs, lo-fi, etc.
   - Adventure.ogg end-track could also vary by active folder set

3. Move OSRS Sounds folder to second hard drive once set up
   - Update the soundsDir path in TimerOverlay.ahk (line ~38) to point to new location

--- ATLANTIC LOGISTICS ---

1. New Outlook Inbox Automation Project
   - Auto-delete emails from certain senders after a defined time period
   - Move emails to specific folders after a time period if they meet certain parameters (sender, subject, keywords, etc.)

2. Automation of email information/metadata modification when sending (e.g. auto-populate fields, formatting, signatures based on context)

3. Tools to evaluate:
   - Power Automate (primary candidate — already in Microsoft ecosystem)
   - Look into what they are currently using for any existing automation
   - RPA (Robotic Process Automation) — e.g. UiPath, Automation Anywhere, Power Automate Desktop
   - DPA (Digital Process Automation) — broader workflow orchestration, evaluate fit vs RPA for this use case

--- PERSONAL ---

AHK / Claude Setup:
1. Look into building AHK MCP introspection server
   - Would give Claude structured on-demand access to AHK function catalog and Caster rules
   - Better than pre-loading memory files when catalog gets large — fetches only what's needed
   - Built with FastMCP (Python 3), registered via: claude mcp add ...
   - Full concept, code skeleton, trade-offs, and gotchas documented in:
     TEXTDOCS/programming/Debugging/AHK_MCP_INTROSPECTION_SERVER.txt
   - Not urgent — do this when verify.ps1 + memory files start feeling unwieldy

9. Timer App (Scripts/TimerApp/) — finish and polish
   - Test all voice commands end-to-end after enabling Timer Rule
     Say "enable Timer Rule" to activate, then:
       "set <n> minute timer" / "set <n> second timer"
       "set alarm <hour> <minute>"
       "start work/meditation/break timer"
       "show timers" / "cancel all timers"
     Voice commands file: caster/rules/timer_commands.py
     UI: http://localhost:7459
   - Try out the generated sounds, replace with better ones if needed
   - Add more presets as needed (config.json or settings panel)
   - Consider adding Stream Deck buttons for common timers

1. Google Docs — combo keypress timing bug
  Combination voice commands (e.g. "lefty clear to sauce") fire keypresses so fast that
  Google Docs interprets them as Ctrl+Home/Ctrl+End/etc., jumping to start/end of document.
  Need to add delays between keypresses in the relevant AHK functions, or investigate
  whether SetKeyDelay needs to be applied specifically for Google Docs context.

1. Improve Google voice commands
   a. Fix "text <name>" for contacts not on first page — URL-based navigation (itemId) fails
      when the contact's thread isn't in the lazy-loaded message list. Switch to search-based
      approach: click search bar, type contact name, click result. See TODO in GoogleVoice.ahk.

2.  look into more ways to add words to Dragon. I think my  current flow for that is really bad. import vocabulary??? 

3. Fix downloads folder and figure out hard drive situation / Recover stuff off my 1 TB hard drive.
   - Once second drive is set up: move OSRS Sounds folder there and update soundsDir in TimerOverlay.ahk
   - Move CountdownTimer tooltip images (Scripts/CountdownTimer/wow_items/tooltips/) to non-SSD drive
     ~700+ PNG files, will keep growing. Update the tooltip path in TimerOverlay.ahk to match.

2. Continue improving new tab page
   a. Make a version that has tasks that you can check off as completed and they will reset to zero days. So like journaling might say five days
   b. User profiles (with Google integration)
   c. Image debugging workflow
   d. General settings page
   e. Make new panel of like my current YouTube links or like current tabs/activities/etc?
   f. Make my new tab page work from anywhere? At least speech
   g. Make my new tab page able to implement things like a poem a day or quote of the day. And make it dynamically pulled from other websites.
   h. Possibly even a weather panel?
   i. For different panels like that make them easy to add as different options to different boards.
   j. Make a specific panel type where I can have recurring events or tasks and then I just check them and it resets the timer. So like journaling it'll say five days or something.
   k. Possibly make my to do list live portably from somewhere else? maybe
   l. Make it so that my checklists can be nested, so that I can click on one and it will have tasks inside of it.


8. Fix Dragon/Natlink stability — kill/restart and freezing
   - Current kill is unsafe — investigate a cleaner approach
   - Options: Dragon's own shutdown API, UI automation (exit gracefully through Dragon menus), or staged kill that waits for Natlink to unload
   - Also hide/close the Natlink window as part of the shutdown sequence
   - Goal: reliable safe kill that avoids the frozen-state backlog problem and leaves no orphaned processes
   - Diagnostic info in Dragon_NATLINK_debugging.txt
